## 1. Мягкая отмена: модель, время, конфликт и выдача

- [x] 1.1 Тесты (red): адаптировать хелперы под `NewStore(now, restoreWindow)`
  с фиксированным `now`; добавить кейсы «создание поверх отменённой → 201,
  в выдаче ровно одна новая запись» и «после отмены GET возвращает 0 записей
  по этой брони» (сценарии «Создание поверх отменённой», «Выдача после отмены»).
  Зафиксировать наблюдаемый red
- [x] 1.2 Реализация: поле `CancelledAt *time.Time` с `json:"-"` в `Booking`,
  сигнатура `NewStore(now func() time.Time, restoreWindow time.Duration)`,
  `Delete` ставит `CancelledAt` вместо удаления, `Create` и `ListByDate`
  пропускают отменённые. `make test` зелёный

## 2. Восстановление: базовые статусы

- [x] 2.1 Тесты (red): `POST /bookings/{id}/restore` — успех в середине окна
  (200, тело из четырёх полей, бронь снова в выдаче), ровно на границе окна
  (200), неизвестный id (404), окно истекло на минуту (410, бронь остаётся
  отменённой, повторный restore тоже 410), restore активной брони (400,
  без изменений), повторный restore после успеха (400). Зафиксировать red
- [x] 2.2 Реализация: sentinel-ошибки `ErrNotCancelled`, `ErrRestoreExpired`,
  метод `Store.Restore` (проверка окна `now.Sub(cancelledAt) > restoreWindow`,
  сброс `CancelledAt` под одним локом), хендлер и роут
  `POST /bookings/{id}/restore` с маппингом 404/400/410/409. `make test` зелёный

## 3. Конфликт восстановления и новое окно

- [x] 3.1 Тесты (red): слот занят частично пересекающейся бронью → 409,
  обе брони неизменны (в выдаче только новая, повторный restore — 409);
  соседняя бронь вплотную (начало = конец) → 200, в выдаче две записи;
  cancel → restore → cancel → restore по свежему окну → 200. Зафиксировать red
- [x] 3.2 Реализация: проверка пересечения с активными бронями комнаты
  внутри `Store.Restore` до сброса `CancelledAt`. `make test` зелёный

## 4. Настройка RESTORE_WINDOW

- [x] 4.1 Тесты (red): `parseRestoreWindow` в `cmd/booking` — пустая строка → 10m,
  валидное значение (`"90s"`, `"1h"`) → распарсено, невалидное (`"abc"`) → ошибка. Зафиксировать red
- [x] 4.2 Реализация: `parseRestoreWindow(raw string) (time.Duration, error)`
  через `time.ParseDuration`; `main.go` читает `RESTORE_WINDOW`, при ошибке —
  `log.Fatalf` до старта сервера. `make test` зелёный

## 5. Завершение

- [x] 5.1 Самоаудит: таблица «сценарий спеки → конкретный тест», все сценарии
  из specs/bookings/spec.md покрыты, непокрытых нет
- [x] 5.2 Полный `make ai-check SPEC_LINT=1` — зелёный, результат зафиксирован
  реальным запуском
